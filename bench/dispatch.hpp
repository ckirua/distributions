#pragma once
// Generated by tools/codegen_distributions.py — do not edit by hand.

#include "distributions/all.hpp"

#include <cstdint>
#include <cstring>
#include <fstream>
#include <stdexcept>
#include <string>
#include <vector>

namespace bench {

struct DistSpec {
    std::string id;
    bool discrete{true};
};

inline const std::vector<DistSpec>& all_dists() {
    static const std::vector<DistSpec> k = {
        {"benford", true},
        {"bernoulli", true},
        {"beta-binomial", true},
        {"binomial", true},
        {"categorical", true},
        {"hypergeometric", true},
        {"negative-hypergeometric", true},
        {"poisson-binomial", true},
        {"rademacher", true},
        {"soliton", true},
        {"discrete-uniform", true},
        {"zipf", true},
        {"zipfmandelbrot", true},
        {"beta-negative-binomial", true},
        {"borel", true},
        {"conwaymaxwellpoisson", true},
        {"discrete-phase-type", true},
        {"delaporte", true},
        {"extended-negative-binomial", true},
        {"floryschulz", true},
        {"gausskuzmin", true},
        {"geometric", true},
        {"logarithmic", true},
        {"mixed-poisson", true},
        {"negative-binomial", true},
        {"panjer", true},
        {"parabolic-fractal", true},
        {"poisson", true},
        {"skellam", true},
        {"yulesimon", true},
        {"zeta", true},
        {"arcsine", false},
        {"argus", false},
        {"baldingnichols", false},
        {"bates", false},
        {"beta", false},
        {"generalized-beta", false},
        {"beta-rectangular", false},
        {"continuous-bernoulli", false},
        {"continuous-binomial", false},
        {"irwinhall", false},
        {"kumaraswamy", false},
        {"logit-normal", false},
        {"noncentral-beta", false},
        {"pert", false},
        {"raised-cosine", false},
        {"reciprocal", false},
        {"triangular", false},
        {"u-quadratic", false},
        {"uniform", false},
        {"wigner-semicircle", false},
        {"benini", false},
        {"benktander-1st-kind", false},
        {"benktander-2nd-kind", false},
        {"beta-prime", false},
        {"burr", false},
        {"chi", false},
        {"chi-squared", false},
        {"noncentral-chi-squared", false},
        {"inverse-chi-squared", false},
        {"scaled-inverse-chi-squared", false},
        {"dagum", false},
        {"davis", false},
        {"erlang", false},
        {"hyper", false},
        {"exponential", false},
        {"hyperexponential", false},
        {"hypoexponential", false},
        {"exponential-logarithmic", false},
        {"f", false},
        {"noncentral-f", false},
        {"folded-normal", false},
        {"frechet", false},
        {"gamma", false},
        {"generalized-gamma", false},
        {"inverse-gamma", false},
        {"gamma-gompertz", false},
        {"gompertz", false},
        {"shifted", false},
        {"half-logistic", false},
        {"half-normal", false},
        {"hotellings-t-squared", false},
        {"hartmanwatson", false},
        {"inverse-gaussian", false},
        {"generalized", false},
        {"kolmogorov", false},
        {"levy", false},
        {"log-cauchy", false},
        {"log-laplace", false},
        {"log-logistic", false},
        {"log-normal", false},
        {"log-t", false},
        {"lomax", false},
        {"matrix-exponential", false},
        {"maxwellboltzmann", false},
        {"maxwelljuttner", false},
        {"mittag-leffler", false},
        {"nakagami", false},
        {"pareto", false},
        {"phase-type", false},
        {"poly-weibull", false},
        {"rayleigh", false},
        {"relativistic-breitwigner", false},
        {"rice", false},
        {"truncated-normal", false},
        {"type-2-gumbel", false},
        {"weibull", false},
        {"discrete-weibull", false},
        {"wilkss-lambda", false},
        {"cauchy", false},
        {"exponential-power", false},
        {"fishers-z", false},
        {"kaniadakis-gaussian", false},
        {"gaussian-q", false},
        {"generalized-hyperbolic", false},
        {"generalized-logistic-logistic-beta", false},
        {"generalized-normal", false},
        {"geometric-stable", false},
        {"gumbel", false},
        {"holtsmark", false},
        {"hyperbolic-secant", false},
        {"johnsons-su", false},
        {"landau", false},
        {"laplace", false},
        {"asymmetric", false},
        {"logistic", false},
        {"noncentral-t", false},
        {"normal", false},
        {"normal-inverse-gaussian", false},
        {"skew-normal", false},
        {"slash", false},
        {"stable", false},
        {"student-t", false},
        {"tracywidom", false},
        {"variance-gamma", false},
        {"voigt", false},
        {"generalized-chi-squared", false},
        {"generalized-extreme-value", false},
        {"generalized-pareto", false},
        {"marchenkopastur", false},
        {"kaniadakis-exponential", false},
        {"kaniadakis-gamma", false},
        {"kaniadakis-weibull", false},
        {"kaniadakis-logistic", false},
        {"kaniadakis-erlang", false},
        {"q-exponential", false},
        {"q-gaussian", false},
        {"q-weibull", false},
        {"shifted-log-logistic", false},
        {"tukey-lambda", false},
        {"rectified-gaussian", false},
        {"ewens", false},
        {"multinomial", false},
        {"dirichlet-multinomial", false},
        {"negative-multinomial", false},
        {"dirichlet", false},
        {"generalized-dirichlet", false},
        {"multivariate-laplace", false},
        {"multivariate-normal", false},
        {"multivariate-stable", false},
        {"multivariate-t", false},
        {"normal-gamma", false},
        {"normal-inverse-gamma-distribution", false},
        {"lkj", false},
        {"matrix-beta", false},
        {"matrix-f", false},
        {"matrix-normal", false},
        {"matrix-t", false},
        {"matrix-gamma", false},
        {"inverse-matrix-gamma-distribution", false},
        {"wishart", false},
        {"normal-wishart", false},
        {"inverse-wishart", false},
        {"normal-inverse", false},
        {"complex", false},
        {"uniform-distribution-on-a-stiefel-manifold", false},
        {"circular-uniform", false},
        {"univariate-von-mises", false},
        {"wrapped-normal", false},
        {"wrapped-cauchy", false},
        {"wrapped-exponential", false},
        {"wrapped-asymmetric-laplace", false},
        {"wrapped-levy", false},
        {"kent", false},
        {"bivariate-von-mises", false},
        {"von-misesfisher", false},
        {"bingham", false},
        {"dirac-delta-function", false},
        {"cantor", false},
    };
    return k;
}

inline void sample_cpp(
    const std::string& dist,
    std::uint64_t seed,
    std::size_t n,
    std::vector<int>& out_i,
    std::vector<double>& out_d) {
    distributions::Pcg32 rng(seed);
    if (dist == "benford") {
        const distributions::Benford d;
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "bernoulli") {
        const distributions::Bernoulli d(0.35);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "beta-binomial") {
        const distributions::BetaBinomial d(20, 2.0, 5.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "binomial") {
        const distributions::Binomial d(20, 0.4);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "categorical") {
        const distributions::Categorical d({0.5, 0.3, 0.2});
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "hypergeometric") {
        const distributions::Hypergeometric d(50, 10, 5);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "negative-hypergeometric") {
        const distributions::NegativeHypergeometric d(50, 10, 5);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "poisson-binomial") {
        const distributions::PoissonBinomial d({0.1, 0.2, 0.3, 0.4});
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "rademacher") {
        const distributions::Rademacher d;
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "soliton") {
        const distributions::Soliton d(10);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "discrete-uniform") {
        const distributions::DiscreteUniform d(1, 7);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "zipf") {
        const distributions::Zipf d(10, 2.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "zipfmandelbrot") {
        const distributions::ZipfMandelbrot d(10, 1.0, 2.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "beta-negative-binomial") {
        const distributions::BetaNegativeBinomial d(2.0, 5.0, 1.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "borel") {
        const distributions::Borel d(0.5);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "conwaymaxwellpoisson") {
        const distributions::Conwaymaxwellpoisson d(4.0, 1.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "discrete-phase-type") {
        const distributions::DiscretePhaseType d(0.6, 0.4);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "delaporte") {
        const distributions::Delaporte d(1.0, 2.0, 0.5);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "extended-negative-binomial") {
        const distributions::ExtendedNegativeBinomial d(5.0, 0.4);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "floryschulz") {
        const distributions::Floryschulz d(0.8);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "gausskuzmin") {
        const distributions::Gausskuzmin d;
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "geometric") {
        const distributions::Geometric d(0.25);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "logarithmic") {
        const distributions::Logarithmic d(0.5);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "mixed-poisson") {
        const distributions::MixedPoisson d(2.0, 1.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "negative-binomial") {
        const distributions::NegativeBinomial d(5, 0.4);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "panjer") {
        const distributions::Panjer d(4.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "parabolic-fractal") {
        const distributions::ParabolicFractal d(2.0, 0.1);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "poisson") {
        const distributions::Poisson d(4.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "skellam") {
        const distributions::Skellam d(3.0, 1.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "yulesimon") {
        const distributions::Yulesimon d(2.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "zeta") {
        const distributions::Zeta d(100, 2.0);
        out_i.resize(n);
        d.sample_batch(out_i.data(), n, rng);
        return;
    }
    if (dist == "arcsine") {
        const distributions::Arcsine d;
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "argus") {
        const distributions::Argus d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "baldingnichols") {
        const distributions::Baldingnichols d(0.1);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "bates") {
        const distributions::Bates d(4, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "beta") {
        const distributions::Beta d(2.0, 5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-beta") {
        const distributions::GeneralizedBeta d(2.0, 5.0, 0.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "beta-rectangular") {
        const distributions::BetaRectangular d(2.0, 5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "continuous-bernoulli") {
        const distributions::ContinuousBernoulli d(0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "continuous-binomial") {
        const distributions::ContinuousBinomial d(10);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "irwinhall") {
        const distributions::Irwinhall d(4, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kumaraswamy") {
        const distributions::Kumaraswamy d(2.0, 5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "logit-normal") {
        const distributions::LogitNormal d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "noncentral-beta") {
        const distributions::NoncentralBeta d(2.0, 5.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "pert") {
        const distributions::Pert d(0.0, 0.5, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "raised-cosine") {
        const distributions::RaisedCosine d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "reciprocal") {
        const distributions::Reciprocal d(0.5, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "triangular") {
        const distributions::Triangular d(0.0, 0.5, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "u-quadratic") {
        const distributions::UQuadratic d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "uniform") {
        const distributions::Uniform d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wigner-semicircle") {
        const distributions::WignerSemicircle d(-1.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "benini") {
        const distributions::Benini d(1.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "benktander-1st-kind") {
        const distributions::Benktander1stKind d(1.0, 1.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "benktander-2nd-kind") {
        const distributions::Benktander2ndKind d(1.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "beta-prime") {
        const distributions::BetaPrime d(2.0, 5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "burr") {
        const distributions::Burr d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "chi") {
        const distributions::Chi d(3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "chi-squared") {
        const distributions::ChiSquared d(3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "noncentral-chi-squared") {
        const distributions::NoncentralChiSquared d(3.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "inverse-chi-squared") {
        const distributions::InverseChiSquared d(3.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "scaled-inverse-chi-squared") {
        const distributions::ScaledInverseChiSquared d(3.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "dagum") {
        const distributions::Dagum d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "davis") {
        const distributions::Davis d(3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "erlang") {
        const distributions::Erlang d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "hyper") {
        const distributions::Hyper d(1.0, 2.0, 0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "exponential") {
        const distributions::Exponential d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "hyperexponential") {
        const distributions::Hyperexponential d(0.5, 1.0, 0.6);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "hypoexponential") {
        const distributions::Hypoexponential d(2.0, 3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "exponential-logarithmic") {
        const distributions::ExponentialLogarithmic d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "f") {
        const distributions::FDistribution d(5.0, 10.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "noncentral-f") {
        const distributions::NoncentralF d(5.0, 10.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "folded-normal") {
        const distributions::FoldedNormal d(1.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "frechet") {
        const distributions::Frechet d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "gamma") {
        const distributions::Gamma d(2.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-gamma") {
        const distributions::GeneralizedGamma d(2.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "inverse-gamma") {
        const distributions::InverseGamma d(3.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "gamma-gompertz") {
        const distributions::GammaGompertz d(2.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "gompertz") {
        const distributions::Gompertz d(1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "shifted") {
        const distributions::Shifted d(1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "half-logistic") {
        const distributions::HalfLogistic d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "half-normal") {
        const distributions::HalfNormal d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "hotellings-t-squared") {
        const distributions::HotellingsTSquared d(5.0, 10.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "hartmanwatson") {
        const distributions::Hartmanwatson d(3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "inverse-gaussian") {
        const distributions::InverseGaussian d(1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized") {
        const distributions::Generalized d(2.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kolmogorov") {
        const distributions::Kolmogorov d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "levy") {
        const distributions::Levy d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "log-cauchy") {
        const distributions::LogCauchy d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "log-laplace") {
        const distributions::LogLaplace d(1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "log-logistic") {
        const distributions::LogLogistic d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "log-normal") {
        const distributions::LogNormal d(0.0, 0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "log-t") {
        const distributions::LogT d(5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "lomax") {
        const distributions::Lomax d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "matrix-exponential") {
        const distributions::MatrixExponential d(1.0, 2.0, 3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "maxwellboltzmann") {
        const distributions::MaxwellBoltzmann d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "maxwelljuttner") {
        const distributions::Maxwelljuttner d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "mittag-leffler") {
        const distributions::MittagLeffler d(0.8, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "nakagami") {
        const distributions::Nakagami d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "pareto") {
        const distributions::Pareto d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "phase-type") {
        const distributions::PhaseType d(1.0, 2.0, 4.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "poly-weibull") {
        const distributions::PolyWeibull d(1.5, 2.5, 0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "rayleigh") {
        const distributions::Rayleigh d(1.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "relativistic-breitwigner") {
        const distributions::RelativisticBreitwigner d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "rice") {
        const distributions::Rice d(0.5, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "truncated-normal") {
        const distributions::TruncatedNormal d(-1.0, 2.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "type-2-gumbel") {
        const distributions::Type2Gumbel d(1.5, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "weibull") {
        const distributions::Weibull d(1.5, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "discrete-weibull") {
        const distributions::DiscreteWeibull d(1.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wilkss-lambda") {
        const distributions::WilkssLambda d(3.0, 5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "cauchy") {
        const distributions::Cauchy d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "exponential-power") {
        const distributions::ExponentialPower d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "fishers-z") {
        const distributions::FishersZ d(10.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kaniadakis-gaussian") {
        const distributions::KaniadakisGaussian d(0.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "gaussian-q") {
        const distributions::GaussianQ d(1.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-hyperbolic") {
        const distributions::GeneralizedHyperbolic d(0.0, 1.5, 0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-logistic-logistic-beta") {
        const distributions::GeneralizedLogisticLogisticBeta d(1.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-normal") {
        const distributions::GeneralizedNormal d(1.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "geometric-stable") {
        const distributions::GeometricStable d(1.5, 0.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "gumbel") {
        const distributions::Gumbel d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "holtsmark") {
        const distributions::Holtsmark d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "hyperbolic-secant") {
        const distributions::HyperbolicSecant d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "johnsons-su") {
        const distributions::JohnsonsSu d(0.5, 1.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "landau") {
        const distributions::Landau d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "laplace") {
        const distributions::Laplace d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "asymmetric") {
        const distributions::Asymmetric d(2.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "logistic") {
        const distributions::Logistic d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "noncentral-t") {
        const distributions::NoncentralT d(5.0, 2.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "normal") {
        const distributions::Normal d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "normal-inverse-gaussian") {
        const distributions::NormalInverseGaussian d(1.5, 0.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "skew-normal") {
        const distributions::SkewNormal d(0.0, 1.0, 4.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "slash") {
        const distributions::Slash d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "stable") {
        const distributions::Stable d(1.5, 0.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "student-t") {
        const distributions::StudentT d(5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "tracywidom") {
        const distributions::Tracywidom d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "variance-gamma") {
        const distributions::VarianceGamma d(1.0, 0.0, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "voigt") {
        const distributions::Voigt d(1.0, 1.0, 0.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-chi-squared") {
        const distributions::GeneralizedChiSquared d(3.0, 1.0, 2.0, 0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-extreme-value") {
        const distributions::GeneralizedExtremeValue d(0.1, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-pareto") {
        const distributions::GeneralizedPareto d(0.1, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "marchenkopastur") {
        const distributions::Marchenkopastur d(1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kaniadakis-exponential") {
        const distributions::KaniadakisExponential d(0.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kaniadakis-gamma") {
        const distributions::KaniadakisGamma d(0.5, 2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kaniadakis-weibull") {
        const distributions::KaniadakisWeibull d(0.5, 1.5, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kaniadakis-logistic") {
        const distributions::KaniadakisLogistic d(0.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kaniadakis-erlang") {
        const distributions::KaniadakisErlang d(0.5, 3.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "q-exponential") {
        const distributions::QExponential d(0.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "q-gaussian") {
        const distributions::QGaussian d(1.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "q-weibull") {
        const distributions::QWeibull d(0.5, 1.5, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "shifted-log-logistic") {
        const distributions::ShiftedLogLogistic d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "tukey-lambda") {
        const distributions::TukeyLambda d(0.14, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "rectified-gaussian") {
        const distributions::RectifiedGaussian d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "ewens") {
        const distributions::Ewens d(2.0, 10);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "multinomial") {
        const distributions::Multinomial d(10, 0.4);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "dirichlet-multinomial") {
        const distributions::DirichletMultinomial d(10, 2.0, 3.0, 5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "negative-multinomial") {
        const distributions::NegativeMultinomial d(2.0, 0.4, 0.3, 0.3);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "dirichlet") {
        const distributions::Dirichlet d(2.0, 3.0, 5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "generalized-dirichlet") {
        const distributions::GeneralizedDirichlet d(2.0, 3.0, 2.0, 3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "multivariate-laplace") {
        const distributions::MultivariateLaplace d(1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "multivariate-normal") {
        const distributions::MultivariateNormal d(0.2);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "multivariate-stable") {
        const distributions::MultivariateStable d(1.5, 0.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "multivariate-t") {
        const distributions::MultivariateT d(5.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "normal-gamma") {
        const distributions::NormalGamma d(2.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "normal-inverse-gamma-distribution") {
        const distributions::NormalInverseGammaDistribution d(3.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "lkj") {
        const distributions::Lkj d(2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "matrix-beta") {
        const distributions::MatrixBeta d(2.0, 3.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "matrix-f") {
        const distributions::MatrixF d(5.0, 8.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "matrix-normal") {
        const distributions::MatrixNormal d(1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "matrix-t") {
        const distributions::MatrixT d(5.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "matrix-gamma") {
        const distributions::MatrixGamma d(3.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "inverse-matrix-gamma-distribution") {
        const distributions::InverseMatrixGammaDistribution d(3.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wishart") {
        const distributions::Wishart d(5.0, 1.0, 0.2, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "normal-wishart") {
        const distributions::NormalWishart d(5.0, 1.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "inverse-wishart") {
        const distributions::InverseWishart d(6.0, 1.0, 0.2, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "normal-inverse") {
        const distributions::NormalInverse d(6.0, 1.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "complex") {
        const distributions::Complex d(5.0, 1.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "uniform-distribution-on-a-stiefel-manifold") {
        const distributions::UniformDistributionOnAStiefelManifold d;
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "circular-uniform") {
        const distributions::CircularUniform d(0.0, 0.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "univariate-von-mises") {
        const distributions::UnivariateVonMises d(0.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wrapped-normal") {
        const distributions::WrappedNormal d(0.0, 0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wrapped-cauchy") {
        const distributions::WrappedCauchy d(0.5, 0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wrapped-exponential") {
        const distributions::WrappedExponential d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wrapped-asymmetric-laplace") {
        const distributions::WrappedAsymmetricLaplace d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "wrapped-levy") {
        const distributions::WrappedLevy d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "kent") {
        const distributions::Kent d(2.0, 0.5);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "bivariate-von-mises") {
        const distributions::BivariateVonMises d(2.0, 2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "von-misesfisher") {
        const distributions::VonMisesfisher d(2.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "bingham") {
        const distributions::Bingham d(-1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "dirac-delta-function") {
        const distributions::DiracDelta d(0.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    if (dist == "cantor") {
        const distributions::Cantor d(0.0, 1.0);
        out_d.resize(n);
        d.sample_batch(out_d.data(), n, rng);
        return;
    }
    throw std::runtime_error("unknown dist: " + dist);
}

inline void write_samples_binary(
    const std::string& path,
    bool discrete,
    const std::vector<int>& out_i,
    const std::vector<double>& out_d) {
    std::ofstream f(path, std::ios::binary);
    if (!f) {
        throw std::runtime_error("cannot open " + path);
    }
    const char tag = discrete ? 'i' : 'd';
    f.write(&tag, 1);
    const std::uint64_t n = discrete ? out_i.size() : out_d.size();
    f.write(reinterpret_cast<const char*>(&n), sizeof(n));
    if (discrete) {
        f.write(reinterpret_cast<const char*>(out_i.data()), static_cast<std::streamsize>(n * sizeof(int)));
    } else {
        f.write(reinterpret_cast<const char*>(out_d.data()), static_cast<std::streamsize>(n * sizeof(double)));
    }
}

}  // namespace bench
